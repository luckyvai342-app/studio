
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User account and battle identity",
      "type": "object",
      "properties": {
        "username": { "type": "string", "description": "App display name" },
        "gameUsername": { "type": "string", "description": "Free Fire In-game Name" },
        "gameUid": { "type": "string", "description": "Free Fire numeric UID" },
        "gameRegion": { "type": "string", "enum": ["India", "Bangladesh", "Pakistan", "Nepal"] },
        "accountVerified": { "type": "boolean" },
        "status": { "type": "string", "enum": ["active", "suspended", "banned"] },
        "phone": { "type": "string" },
        "walletBalance": { "type": "number" },
        "lastUidUpdate": { "type": "string", "format": "date-time" },
        "lastActionAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["username", "gameUid", "phone", "walletBalance"]
    },
    "Tournament": {
      "title": "Tournament",
      "description": "Match details and prize information",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "entryFee": { "type": "number" },
        "totalPrize": { "type": "number" },
        "status": { "type": "string", "enum": ["open", "ongoing", "completed", "cancelled"] },
        "startTime": { "type": "string", "format": "date-time" },
        "maxPlayers": { "type": "number" },
        "joinedCount": { "type": "number" },
        "map": { "type": "string" },
        "type": { "type": "string", "enum": ["Solo", "Duo", "Squad"] },
        "imageUrl": { "type": "string" },
        "roomId": { "type": "string" },
        "roomPassword": { "type": "string" },
        "resultsUploaded": { "type": "boolean" }
      }
    },
    "Transaction": {
      "title": "Transaction",
      "description": "Financial records for deposits and fees",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "amount": { "type": "number" },
        "type": { "type": "string", "enum": ["deposit", "withdrawal", "entry_fee", "prize", "refund"] },
        "status": { "type": "string", "enum": ["pending", "completed", "failed"] },
        "referenceId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "amount", "type", "status", "createdAt"]
    },
    "AdminLog": {
      "title": "Admin Log",
      "description": "Audit trail for admin actions",
      "type": "object",
      "properties": {
        "adminId": { "type": "string" },
        "action": { "type": "string" },
        "targetId": { "type": "string" },
        "details": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/entities/UserProfile",
      "description": "User profile and battle identity"
    },
    "/tournaments/{tournamentId}": {
      "schema": "#/entities/Tournament",
      "description": "Main tournament metadata"
    },
    "/tournaments/{tournamentId}/participants/{userId}": {
      "schema": {
        "type": "object",
        "properties": {
          "username": { "type": "string" },
          "gameUid": { "type": "string" },
          "gameUsername": { "type": "string" },
          "kills": { "type": "number" },
          "placement": { "type": "number" },
          "prizeWon": { "type": "number" },
          "joinedAt": { "type": "string", "format": "date-time" }
        }
      },
      "description": "Participants list with game identity"
    },
    "/audit_logs/{logId}": {
      "schema": "#/entities/AdminLog",
      "description": "System-wide audit logs"
    }
  }
}
